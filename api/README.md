# API (placeholder)

Serverless or edge handlers will live here (e.g. framework-specific `functions/` or provider routes). The React app uses [`src/lib/api.ts`](../src/lib/api.ts) as the client boundary until endpoints exist.
